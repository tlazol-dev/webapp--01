# Getting Started

(1) Clone the repository

(2) Remove the origin

(3) Add Packages

(4) Run the server:

- In Development (runs with nodemon)
  ```sh
  npm run dev
  ```

- In Production 
  ```sh
  npm start
  ```
